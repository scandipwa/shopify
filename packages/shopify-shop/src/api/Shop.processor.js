/**
 * Shop processor
 * @extPoint Great place to add some "calculated" field into shop
 * @extExample ([shop], callback) => {
 *      callback(shop);
 *      shop.translatedDescription = translate(shop.description);
 * }
 * @namespace ShopifyShop/Api/Shop/Processor/processShop */
export const processShop = () => {};
