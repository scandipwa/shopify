import { createContext } from 'react';

// eslint-disable-next-line @scandipwa/scandipwa-guidelines/export-level-one
const lineItemsContext = createContext({});
lineItemsContext.displayName = 'ShopifyLineItems';
export default lineItemsContext;
